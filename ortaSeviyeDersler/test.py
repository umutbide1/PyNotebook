import myModuls
import numbers
import numpy as np  # inanılmaz garip bir hata çözdüm 
                    # dosyada içerisinde numbers adında bir .py uzantılı dosya olunca hata veriyordu kütüphane ismini değiştidim düzeldi
                    # içersindeki integral kütüphanesi de bu değişkeni ya da modülü kullanıyor zannedersem ondan dolayı böyle bir hata var  
result= help(myModuls)
result= help(np)
 

result=help(numbers)